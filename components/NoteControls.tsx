import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const NoteControls = () => {
  return (
    <View>
      <Text>NoteControls</Text>
    </View>
  );
};

export default NoteControls;

const styles = StyleSheet.create({});
